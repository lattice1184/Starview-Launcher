# 更新日志

## v1.1.0（2026-08-23）

### 自动修复

- 启动失败原因不再只有红字+一闪而过的 Toast：自动修复失败时弹崩溃窗正大光明展示真实原因（含一键修复）
- 自动修复读取当时启动日志（launch-*.log）执行诊断下载，不再只靠内存控制台
- 修复失败发布全局事件，任何路径的修复失败都有提示
- 0 字节/损坏的客户端 jar 不再被「文件已完整」跳过（存在性快路径补空文件校验）

### 版本与实例

- 加载器覆盖原版：装 Fabric/Forge 后同版本已装原版自动降级隐藏（合并为唯一条目）；删加载器时连带清理被吸收的原版目录（多加载器共享时保留）
- 主页版本下拉自动选中「最新安装的版本」——下载模组「跟随实例」不再落错实例（TACZ 落 26.1 的根因之一）

### 安装交互

- 安装直接选目录：点「安装」直接弹系统目录选择器（默认指向对应实例 mods 文件夹），取消即不装；不再「先选实例下拉再填路径文本框」两步
- 「匹配文件」下载同样强制选目标（未选实例拦截提示，不再静默落到 downloads 缓存）
- 整合包豁免：装独立实例 downloads/modpacks，不弹目录选择器
- 修复安装路径二次拼接的嵌套 bug（`...\mods\versions\{id}\mods`），用户改/选其它实例目录也正确落盘

## v1.0.9（2026-08-23）

### 修复

- 大文件下载「假成功」根治：下载完成提示后目标文件可能不存在（字节满即完成分支在分片合并阶段误触发，抛异常被吞、合并任务被取消但报成功）——改为仅当 `.race` 文件真实落盘才提前完成，异常只捕获真正的收尾竞态
- 下载历史「位置」按钮支持目录：模组/整合包安装记录不再提示「文件已不存在」，直接打开 mods 文件夹
- 首次设置游戏目录后主页版本列表立即刷新，不用再切页/重启
- 更换游戏目录后版本浏览页自动跟随新目录（文件监听重建），删文件秒同步

### 其他

- 设置-关于新增赞助者名单，感谢每一位支持者的资金支持

## 未发布（开发中）

### 下载

- 陪跑（Pace Runner）：竞速赢家降速时（连续 5 秒不回升且低于窗口次高值一半——次高值不计开局瞬时突发，10MB/s 起速正常回落到 5MB/s 不触发），后台重赛历史最快的 2 个落选源，陪跑源反超并稳定领先 3 秒即顶替旧赢家——不再等判死换路的交替空窗
- 修复性副作用：逐读推拍采样使 headless（无界面）下载同样具备竞速采样与 watchdog 兜底

### 修复

- Forge 整合包下载慢/失败：maven.minecraftforge.net 补上 BMCLAPI 镜像（此前单候选官方直连，国内 37-81KB/s 被判死 2 轮报错）
- 版本设置新增「Java 版本」下拉：一键选择本机已检测的 Java（JDK 25/21/17/16、JRE 8），选中即填入该版本启动配置，仍可手输自定义路径
- 完整性校验补 natives 库：删了 natives jar「修复」不再假报已完整
- 版本详情页新增「资源包 / 光影包」分区：启停（.disabled）、删除、打开文件夹——可下载但无处管理的问题解决
- 已装 mod 行新增「检查更新」：Modrinth 查最新版（版本号对比），有新版一键更新替换，防呆文案（已最新/未找到/检查失败）
- CurseForge 无 Key 时可「前往官网手动下载」（搜索页跳转入口）；下载失败也提供具体模组官网链接
- 内置 CF Key 改构建注入（发布.ps1 从环境变量注入，Key 不进源码仓库）；用户自填 Key 优先，DPAPI 加密落盘

## v1.0.0（2026-08-14）

首个公开发布。

### 下载

- 六源竞速：GitHub 文件同时从官方直链 / ghproxy.net / gh-proxy.com / CDN 签名直链 / 签名直链套镜像抢跑，谁快用谁，历史速度自动排序
- 分片断点续传：中断、换源、重试复用已下分片
- 卡死自救三层：低速判死（30s < 100KB/s）、静默断流读心跳、响应头超时；竞速幸存源停滞 watchdog 兜底
- 下完自动清扫竞速残留

### 登录与皮肤

- 正版设备码登录：配对码自动复制，无需配置 Client ID（多层兜底）
- Littleskin 一条龙：登录、角色引导、皮肤同步
- 离线登录
- 拖拽换肤（自动尺寸校验）；离线皮肤经资源包注入游戏内生效，免模组；一键重置

### 其他

- 单文件自包含 84MB 免安装 + 轻量版 47MB（.NET 10 框架依赖）
- 双击秒开（无启动动画）
- Modrinth + CurseForge 双源模组下载、.zip/.mrpack 整合包拖入导入
- 正版/Littleskin/离线三账号并存与快速切换
- 性能档位、下载限速/分片数/并发可调
- 游戏日志控制台、崩溃日志 zip 导出
