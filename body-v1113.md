## v1.1.13 修复：Mac 一键补齐 Java + 老版本下载修复 + 加载器提速

**Mac Java 自动补齐：**
- 新版 Minecraft 要 Java 25、老版本要 Java 17——检测到缺就自动下载官方运行时，不用自己装
- 装了 Java 却识别不到的问题修复：.jdk 应用包、java_home、Homebrew 装的都能扫到

**老版本下载修复：**
- 1.8.x / 1.12.x 下载报「文件缺失」的问题根治——natives 库不再误报缺文件，老版本能正常下载安装

**加载器提速：**
- 加载器版本列表多源竞速 + 15 秒上限 + 点版本行后台预热，冷缓存也秒出

**其他：** 自动化测试 890+ 项（1 项并发时序用例偶发失败、单独跑通过，非本次改动引入）。

**平台说明：** Windows 用两个 exe（标准版 / 轻量版）；Linux / macOS 用对应 tar.gz，解压后见目录内使用说明。macOS 首次打开如被 Gatekeeper 拦截，右键 → 打开 → 仍要打开。
