## v1.1.14 修复：下载稳定性 + Mac 更新/联机/Java 全修

**下载稳定性（重点）：**
- 下载中改设置不再打断下载——此前动一下限速/并发/下载源，共享连接池重建导致文件下载失败（游戏资源、Java 运行时都可能下不全），启动游戏时直接崩。已修复连接池重建逻辑

**Mac 一键补齐 Java 体验：**
- 自动下载 Java 现在**显示实时进度**，不再停在「检测 Java」像卡死
- 残缺的 Java 运行时（下载中断留下的）会自动补齐，不再启动即崩

**新增「下载 Java」：**
- 设置页可选 Java 25 / 21 / 17 / 8 手动补齐，带进度条，缺哪个下哪个

**Mac 更新覆盖修复：**
- 更新脚本适配 .app 应用包——此前「重启安装」点了没反应、覆盖不了（静默失败），现在正常替换 + 失败会提示原因

**联机功能 Mac 修复：**
- EasyTier / Terracotta 的 macOS 安装包哈希补齐，Mac 能下载安装联机模块了

**其他：**
- 错误报告带上游戏日志（latest.log / crash-reports / JVM 崩溃文件），崩溃更好排查
- 自动化测试 890+ 项

**平台说明：** Windows 用两个 exe（标准版 / 轻量版）；Linux / macOS 用对应 tar.gz，解压后见目录内使用说明。macOS 首次打开如被 Gatekeeper 拦截，右键 → 打开 → 仍要打开。
