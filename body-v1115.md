## v1.1.15 修复：CurseForge 下载 2 项

**CurseForge 镜像生效：**
- 镜像前缀现在对 `mediafilez` / `media` 主机也生效——此前只认旧的 `edge.forgecdn.net` 域名，CurseForge 现在返回的文件直链走 mediafilez，配了 CF 镜像也白配、文件仍单源直连。现在配了镜像前缀会进多源竞速

**下载稳定性：**
- 单个下载源的文件不再被慢速误杀——慢速判死本意是「换源」，单源没得换，之前并发升到顶后被误判死白白报错；现在单源慢也硬啃到底，不再半途失败

**其他：** 自动化测试 890+ 项

**说明：** CurseForge 文件 CDN（mediafilez）对国内直连本身偏慢（约 1MB/s），这是线路问题；配 CF 镜像/加速器能改善。本版修的是「配了镜像却没用上」和「单源被误杀」两个真实缺陷。

**平台说明：** Windows 用两个 exe（标准版 / 轻量版）；Linux / macOS 用对应 tar.gz，解压后见目录内使用说明。macOS 首次打开如被 Gatekeeper 拦截，右键 → 打开 → 仍要打开。
