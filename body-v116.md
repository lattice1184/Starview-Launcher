## v1.1.6

- **沙盒启动模式**：把游戏关进隔离环境跑——Linux 用 bubblewrap 限制文件读写，macOS 用 sandbox-exec，Windows 严格隔离断网走临时防火墙规则（游戏退出自动清理，断网需管理员权限，非管理员自动降级）
- **主页启动旁可选**「普通启动 / 保护模式 / 严格隔离」，即选即存；设置页启动区也能设默认
- **装模组查前置更快**：依赖解析两阶段并行，打开详情 ~2 秒内出前置提示
- **修复**：版本删除后又冒出来（版本页算体积的读路径不再写盘回版）
- **修复**：整合包下载没有大小和进度（文件大小未知时自动回落服务器返回的真实长度）
- 自动化测试 817 项全绿

> Windows 严格隔离的断网依赖管理员权限：普通双击启动器时选严格隔离会自动降级并提示，需右键「以管理员身份运行」后开启。
