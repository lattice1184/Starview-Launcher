## v1.1.7 插件系统

- **插件系统上线**：第三方扩展（`.dll` 实现 `IStarviewPlugin` 接口），左侧导航新增「插件」管理页——导入 / 沙箱试运行 / 停用 / 删除 / 总开关
- **沙箱试运行，先跑给你看**：每个插件可「沙箱试运行」——在独立子进程里跑它的初始化（临时目录重定向到一次性沙盒 + 监听桌面/文档/下载/启动器数据写入 + 10 秒超时强杀），报告它到底写了什么、有没有越界、有没有崩溃挂起。不信任插件自声明，实测后才决定留不留
- **防投毒**：插件 SHA1 登记（首次导入记录，二次比对不一致 = 掉包 → 拒绝加载）；白名单 API（插件只拿到受限上下文，不碰裸系统能力）；删除被占用文件时墓碑式「重启后自动清除」防复活
- **开发者接口**：`examples/plugins/` 含示例插件 + 沙箱探针，`IStarviewPlugin` / `PluginContext`（Log / SettingsDir / Subscribe 事件）白名单
- 诚实边界：Windows 试运行为行为监测（非硬文件系统隔离），写监测范围外的绝对路径可能不捕获；防掉包 + 试运行报告，防不了「你主动装的恶意代码」
- 自动化测试 836 项全绿

> 插件入口：左侧导航「插件」。总开关默认关——导入插件后先「沙箱试运行」，确认干净再启用。
